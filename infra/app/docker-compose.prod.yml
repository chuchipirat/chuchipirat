services:
  app:
    image: nginx:alpine
    container_name: chuchipirat-app-prod
    restart: unless-stopped
    ports:
      - "3011:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - chuchipirat-prod

networks:
  chuchipirat-prod:
    external: true
